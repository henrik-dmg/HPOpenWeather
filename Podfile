project 'OpenWeatherSwift.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
def shared_testing_pods
  pod 'Alamofire'
  pod 'SwiftyJSON'
end

target 'OpenWeatherSwift' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  shared_testing_pods
  # Pods for OpenWeatherSwift

end

target 'OpenWeatherSwiftTests' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  shared_testing_pods
  # Pods for OpenWeatherSwift

end
